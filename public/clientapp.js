const joinButton=document.getElementById('join')
const form_username=document.getElementById('username')
const form_passcode=document.getElementById('passcode')

/*joinButton.addEventListener('click',()=>{
    console.log('clicked')
     console.log('length->'+form_username.value.length)
      console.log('fun '+form_username.value+" da"+form_passcode.value);
       if(form_username.value != "" && form_passcode.value !=""){
  //  window.location.href='/saveddata?room=room1';
                 
       }
      else
       {
           alert('please enter a value ')
       } 

})*/
    // fetch('/saveddata?room='+room+'').then((res)=>{
    //     console.log(res)
    //     res.text().then((data)=>{
    //         console.log(data)

    //     })
    // })
    // console.log(room)